program: mixing_ccleinet_pl.py
method: bayes
metric:
  goal: maximize
  name: Test/val_accuracy
parameters:
  batch_size:
    max: 300
    min: 10
    distribution: int_uniform
  lr:
    max: 0.1
    min: 0.000001
    distribution: uniform
  S:
    max: 5
    min: 1
    distribution: int_uniform
  R:
    max: 5
    min: 1
    distribution: int_uniform
  I:
    max: 5
    min: 1
    distribution: int_uniform
  D:
    max: 9
    min: 3
    distribution: int_uniform
  M:
    max: 5
    min: 1
    distribution: int_uniform
command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}

